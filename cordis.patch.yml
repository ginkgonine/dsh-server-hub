# DSH Server Hub profile bundle.
# Install on the main controller and each monitored remote DSH profile.
- insert:
    - id: dsh-server-hub
      name: dsh-server-hub
      inject: [webServer, connection, timer, sessions]
